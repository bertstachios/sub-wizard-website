# Screenshots needed here

I can *see* the 5 screenshots you attached in chat (rendered inline in
the conversation), but I have no way to pull their actual image bytes
out onto disk — there's no file path or download mechanism for images
pasted directly into a message, only for images that already exist as
files in the project. So `index.html` references the filenames below,
but the files themselves don't exist yet — every screenshot on the
homepage will 404/show a broken image until you save the real PNGs
here yourself (drag them from wherever you have them saved, or re-export
from your device).

Expected filenames, matched to what each screenshot showed:

- `roster.png` — Roster tab, player list (Josh/Aston/Jayden/Lucas/Tim/
  Steve/Patrick/Jack), "+ Add Player", Manage Teams/Manage Rules links
- `manage-rules.png` — Manage Rules screen, "Add a Rule" builder +
  Player Hard Rules list
- `sub-settings.png` — Sub Settings tab, Sub Plan card (Block Time 4:30,
  Subs this game 7), block timeline, Total Game Minutes slider, Playing
  Blocks Per Half (Auto)
- `gameday-setup.png` — Gameday tab before starting, "Tap to mark
  absent" player grid, "Jack is playing 3 blocks in a row" note, Start
  Game button
- `gameday-live.png` — Gameday tab mid-game, Block 1 of 8 timer (4:25),
  Back/Next controls, Next Subs preview, block list

(You also sent a second roster screenshot, identical except for the
status-bar clock — 13:54 instead of 9:41 — that one wasn't used;
`roster.png` above should be whichever roster screenshot you prefer.)
